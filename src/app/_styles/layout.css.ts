import { style } from '@vanilla-extract/css';

export const containerCss = style({
  minHeight: '100vh',
  paddingTop: '120px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
