import { style } from '@vanilla-extract/css';

export const containerCss = style({
  width: '100%',
  minHeight: '100vh',
  paddingTop: '120px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
