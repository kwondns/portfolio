import { Typo } from '@/atoms';

export default function Spinner() {
  return (
    <Typo className="inline-block size-10  animate-spin rounded-full border-[6px] border-stone-300 border-b-transparent" />
  );
}
